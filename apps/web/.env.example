# Public URL the browser uses to reach the API.
# In docker-compose dev, the browser still hits localhost:4000 (host port),
# not the api container — only server-side code talks to the api container by name.
NEXT_PUBLIC_API_URL=http://localhost:4000
